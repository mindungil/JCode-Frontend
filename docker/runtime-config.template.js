window.__JCODE_CONFIG__ = Object.freeze({
  API_URL: "${JCODE_API_URL}",
  KEYCLOAK_URL: "${JCODE_KEYCLOAK_URL}",
  REALM: "${JCODE_REALM}",
  CLIENT_ID: "${JCODE_CLIENT_ID}",
  REDIRECT_URI: "${JCODE_REDIRECT_URI}",
  SCOPE: "${JCODE_SCOPE}"
});
